build/
build_*/
out/
cmake-build-*/
.vs/
.vscode/
.idea/
.claude/
*.vcxproj.user
CMakeSettings.json
*.obj
*.pdb
*.ilk
*.exp
*.lib

# ImGui state now lives at %APPDATA%\DXSense\imgui.ini; an old copy may
# linger in the working dir from a pre-redirect build.
imgui.ini

# /export session transcripts — throwaway notes, not project state.
2026-*.txt

# Per-machine MCP config (absolute Python path; regenerate with `python -m ida_pro_mcp --install claude --scope project`)
.mcp.json

# Local reverse-engineering artefacts (big binaries, IDA databases, extracted assets)
analysis/
*.db
scripts/_*.py
scripts/__pycache__/
